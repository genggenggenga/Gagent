# gagent

A Python coding agent. Built via vibe coding, harness by harness, referencing the projects under `../reference/`.

See `../AGENTS.md` for the workspace workflow and harness registry.

## Quickstart

```bash
uv sync
uv run gagent
uv run pytest
```
